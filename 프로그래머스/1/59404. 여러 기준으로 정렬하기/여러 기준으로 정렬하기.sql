-- Oracle
SELECT
    animal_id,
    name,
    datetime
FROM
    animal_ins
ORDER BY
    name ASC,
    datetime DESC
;

-- MySQL
SELECT
    ANIMAL_ID,
    NAME,
    DATETIME
FROM
    ANIMAL_INS
ORDER BY
    NAME ASC,
    DATETIME DESC
;
