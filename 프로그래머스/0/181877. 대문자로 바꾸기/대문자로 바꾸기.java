class Solution {
    public String solution(String myString) {
        // 정답 선언 및 초기화
        String answer = "";
        // toUpperCase()를 이용해 대문자로 바꾸기
        answer = myString.toUpperCase();
        
        // 정답 리턴
        return answer;
    }
}